# macOS Deployment

This directory holds packaging, signing, notarization, and update-feed scripts for ModelRelay.

See [docs/mac-delivery.md](../../docs/mac-delivery.md) for the release design.

This file is the script-directory entry point. The product-level deployment design lives in `docs/mac-delivery.md`, and the global document map lives in `docs/README.md`.
